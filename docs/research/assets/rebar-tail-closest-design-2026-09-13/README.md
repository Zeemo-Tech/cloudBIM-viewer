# 更接近设计长度：全量验证产物

当前运行 **20260913T124743-1f4446a9**：[本地点云工作台](http://127.0.0.1:8766/?run=20260913T124743-1f4446a9)。[规则与修复记录](../../rebar-tail-closest-design-length-2026-09-13.md)。

固定同一份新运行的内部阶段输入，仅替换尾部模块：与先前硬下限版本相比，**仅 #36 新增移除 10 个断开尾点**。其余点的类别、实例、段和置信度标签逐点一致，原始点簇编号不变，原始内部点移除数为 0。214 个后处理前候选实例均纳入配对审计，最终实例仍为 210 个，无未分配钢筋点。

完整重新计算的 9,216,369 点最终标签与配对回放一致；核验 LAS 全量、1,000,000 点预览以及 354 个瓦片（2,302,885 点）。所有实际裁剪实例的轴向跨度和输出段长均比裁剪前的轴向跨度更接近设计长度。

| 实例 | 设计长度 | 裁前轴向跨度 | 裁后轴向跨度 | 裁后绝对偏差 |
| --- | --- | --- | --- | --- |
| #36 | 3600 mm | 3707.16 mm | 3582.76 mm | 17.24 mm |
| #49 | 3600 mm | 3670.30 mm | 3608.42 mm | 8.42 mm |
| #78 | 280 mm | 499.38 mm | 322.67 mm | 42.67 mm |

#49、#78 是前一版已经裁剪的实例，本次不再增加其裁剪量。#36 的输出段长为 3572.65 mm，与轴向全点跨度采用不同的几何统计方式；本规则统一使用冻结核心轴的全点跨度比较前后结果。

[查看 #36 实际点云裁剪对照图](instance-36-result.png)。红点只作为被移除点的位置对照，不属于新运行保留的钢筋点。此前已逐页查看的[全实例图册](../rebar-tail-review-2026-09-13/README.md)保留；本次通过全点标签比较确认变化仅为 #36，并单独查看其新结果。

机器证据：[新旧规则同输入回放](current-replay-results.json)、[全量导出和长度偏差验证](published-validation.json)、[最新运行元数据](published-run.json)。验证记录包含算法源码 SHA-256。

在仓库根目录复现：

```bash
.cloudbim/mesh-venv/bin/python docs/research/assets/rebar-tail-closest-design-2026-09-13/replay.py --current --run-id 20260913T124743-1f4446a9
.cloudbim/mesh-venv/bin/python docs/research/assets/rebar-tail-closest-design-2026-09-13/verify.py
```

需要已保存的运行数据。`baseline_tails_v2.py` 保存变更前模块，`publish.py` 通过工作台产物写出逻辑离线生成新运行；`plot.py --current` 可渲染全实例双投影图册。中间数组位于忽略目录 `.cloudbim/tail-closest-20260913/current/`。
