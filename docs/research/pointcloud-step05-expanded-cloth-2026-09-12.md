# 第 05 步禁飞区过滤与包络放宽 2 毫米

后续修正：本文的实例保护和有限审查范围仍会留下布外钢筋点，现已由 [第 05 步包络边界漏删修复](pointcloud-strict05-boundary-2026-09-12.md) 取代。包络额外 2 mm 余量保持不变。

按用户最新要求，禁飞区在第 05 步实际过滤悬浮残点，取代上一版仅统计布外候选、未增加删除判定的行为。02A/02B 独立并行计算和底层、顶层、腹杆层保持不变。

包络额外余量设为 0.002 m，仅应用于包络构造，不改变分层参数。横向表面余量由 5 增至 7 mm，上下及腹杆余量由 4 增至 6 mm，弯头表面由 2 增至 4 mm，端部长向由 12 增至 14 mm。显示网格与点包含判定共用放宽后的几何。原有平面、直角边界、弯头内外布结构保留。

05 在已有多视图去噪结果之外，对“位于禁飞区、尚未归属实例、无可靠实测支撑或延续”的残点直接增加删除判定。该规则只接收钢筋审查范围，台面和夹具不参与；已拟合杆件和实测支撑继续保护。01D 和 02A/02B 不提前删点，界面不按原始布外掩码隐藏尚未处理的点。报告新增实际禁飞区过滤数和相对多视图算法的额外删除数。

## 验证

最新运行 `20260911T174450-dce3d804`，版本 `shared-segmentation-v27-step05-expanded-cloth`；按原参数 k=32、16 线程重跑至 06 拓扑辅助整理，流水线耗时 46.36 秒。服务使用项目管理脚本 stop/start 更新。

| 项目 | 结果 |
| --- | --- |
| 源点数量 | 9,216,369，源文件哈希不变 |
| 02A/02B、融合及分层 | 与基线 `20260911T172408-4a614ebe` 的 8 组数组逐点相同 |
| 放宽前 / 后布外候选 | 524,605 / 496,034 |
| 放宽后不再命中的点 | 28,571，无新增布外点 |
| 05 禁飞区实际过滤 | 68,586 |
| 其中相对多视图的额外过滤 | 30,756，与全量新增噪音掩码数量一致 |
| 05 布外保护点 | 17,711 |
| 已拟合钢筋 | 类型与基线保持一致 |
| 06 续算 | 所有上述新增删除点仍为噪音，没有恢复 |

126 项相关 Python 测试、6 组前端检查通过。新增回归用例先复现失败，再验证 05 在多视图未删除时实际执行禁飞区过滤，同时保护实测支撑并排除台面、夹具。另验证包络边界增加 2 mm、临界点包含关系和结构分层不变。浏览器已成功加载最新结果，并显示“额外放宽 2.0 mm”和“禁飞区过滤 / 其中额外删除 68,586 / 30,756”。

全量证据：[validation.json](assets/pointcloud-step05-expanded-cloth-2026-09-12/validation.json)。历史运行未覆盖，修改未提交 Git。
