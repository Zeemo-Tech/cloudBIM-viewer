# 第 05 步包络边界漏删修复

用户反馈第 05 步仍能看到布外点。直接用当前显示包络检查完整源点，确认基线 `20260911T174819-303bb494` 的去噪钢筋中仍有 27,498 个布外点：17,711 点受到已分配实例保护，另外 9,787 点被框带分区或有限审查距离排除在掩码之外。上一版“仅删除无支撑残点”的限制未满足钢筋结果应处于包络内的要求。

本次将第 05 步包络规则改为全部钢筋的硬边界。01D 生成所有非台面源点的布外掩码，不再设置分区或距离豁免；05 仅在已分类为钢筋的点上执行，取消实例、分数、实测支撑及延续豁免。多视图去噪仍有其独立残点审查范围。删除点的实例、拟合段和置信度归零，实例及分段计数重新统计；06 不允许旧高分恢复 05 删除的点。

前置分类和分层不变，包络几何不变，仍包含额外 2 mm 余量。台面和夹具保持各自类别用于对照，05 的“全部钢筋”显示去噪后的钢筋结果；“全部（含噪音）”仍可追溯原始点。

## 验收

新运行 `20260911T180414-b7bbeac4`，版本 `shared-segmentation-v28-step05-steel-boundary`。使用项目管理脚本 stop/start 更新运行服务，按原参数 k=32、16 线程重跑到 06，流水线耗时 41.29 秒。

| 检查 | 结果 |
| --- | --- |
| 源点数 | 9,216,369，源文件哈希不变 |
| 显示包络 | 与基线完整 outerEnvelope 对象一致，额外余量仍为 2 mm |
| 02A、02B、融合分数/证据和分层 | 6 组数组逐点一致，A/B 各 8 线程并行 |
| 05 保留钢筋中的布外点 | 27,498 → 0 |
| 06 保留钢筋中的布外点 | 0 |
| 全部布外钢筋判为噪音 | 103,057 点 |
| 删除点的实例、段号和置信度 | 全部为 0 |
| 实例及分段计数 | 与存储数组逐项一致 |

上述包含关系使用 `inside_outer_envelope` 对完整源点和实际显示包络计算，未使用前端隐藏或缩小预览替代验证。130 项相关 Python 测试和 6 组前端检查通过。新增回归覆盖已拟合高分杆件的布外删除、分区与距离漏判、清除及重统计实例属性、06 高分恢复路径。浏览器成功加载新运行，“05 → 全部钢筋”显示 61,704 个采样点，布外残留计数为 0。

数值证据：[validation.json](assets/pointcloud-strict05-boundary-2026-09-12/validation.json)。历史结果未覆盖，Git 未提交。
