# 台面移除后的设计控制网实验

当前入口为 [点云练武场](http://127.0.0.1:8766/)。选择「台面后控制网 · 粗对齐初始化」或「自动初始化」后，从源 LAS 依次执行法向量、台面移除，再进入 **01B-X 台面后控制网**。本轮止于控制网，不执行分区、分层、禁飞区、几何/投影分类或融合，也不读取旧实例。历史分层后与融合后结果仍显示原来的 01E / 03X 步骤，可从历史列表比较。

拟合候选是台面移除后的全部非台面原始记录。所有设计单元在同一个空间索引上，以设计轴线周围的有限邻域、固定半径、法向量和连续圆柱表面证据拟合；不按底层、顶层、腹杆层划分点云或安排拟合顺序。短筋恢复、平面杂点拒绝、候选歧义、固定设计长度和全量唯一归属检查继续生效。腹杆主体保持直线约束，v12 在主体确认后单独拟合端部弯曲连接；这是构件几何约束，不是点云分层。

台面阶段直接复用已保存的台面掩码，未预处理的源则沿用同一台面平面检测与阈值；跳过投影栅格和临时高度层计算。共享分区、分层、禁飞区属性的全零数组仅为输出兼容占位，报告不含这些步骤；控制网没有语义层统计，界面隐藏层筛选，默认按设计单元标识。

粗对齐模式从保存的位姿开始；自动模式从非台面扫描几何估计控制网位姿。两种模式仍需要服务端设计几何快照，包括钢筋直径、长度和身份关系；“不依赖分层”不表示不依赖设计先验。

输出保留 `control-net.json`、全量 LAS、逐点状态与实例 NPY，以及钢筋、待定、局部离群 LAS。状态 0 为台面、1 为支持钢筋、2 为待定、3 为局部离群；状态 4 仅用于融合排除，本轮没有此类点。源坐标、属性、记录顺序和来源索引保持不变。

## 主体拟合继续提速（2026-09-14，v14）

[打开 v14 结果](http://127.0.0.1:8766/?run=20260914T103758-6e25d4cf)。同一份 **9,216,369 点**源 LAS、相同粗对齐、8 个工作进程预算，完整流程由 **46.49 s 降到 27.44 s**，耗时再减少 **41.0%**。控制网由 **36.85 s 降到 17.79 s**。该计时从源 LAS 重跑法向量、台面处理、主体及弯段拟合，并包含结果保存和预览生成。

| 指标 | v13 | v14 |
| --- | ---: | ---: |
| 控制网整体 | 36.85 s | 17.79 s |
| 其中弯段阶段 | 8.43 s | 8.03 s |
| 全流程 | 46.49 s | 27.44 s |
| 主体 / 已确认曲段 | 210 / 121 | 210 / 121 |
| 曲段新增支持点 | 33,781 | 33,781 |
| 全量支持点 | 1,743,286 | 1,743,286 |

本轮只调整执行方式：圆心候选的径向、法向和覆盖评分批量计算，有限候选组的中位数用选取算法求解；钢筋对先用包围盒距离下界筛掉不可能重复的远距离组合，再执行原来的精确歧义判定；初始主体拟合改用最多 8 个进程，每个任务仅传入至多 30,000 个局部候选点，每批最多 4 倍工作进程数。主进程按原设计顺序消费结果，保留重试、短杆恢复及最终唯一归属顺序。弯段的固定角度基函数预计算一次。模型自由度、采样上限、初值、稳健损失和所有确认阈值保持不变。

独立全量回放与正式结果逐项相同；相对 v13，210 根主体及 121 处弯曲的几何、全部 921 万点状态和实例归属也完全相同。76 项算法与流程测试通过。后台线程启动的进程测试验证了 8 进程上限、每进程一个数值线程、父进程输入隔离，以及正常完成和故意触发工作进程异常后的全部回收。全量及各子集 LAS 的原始字段、来源索引、状态与归属，100 万点预览映射，HTTP 加载器及 Three.js 几何构建均通过。WebGL 交互本轮未实测。

当前余下主要耗时为弯段约 8.03 s、法向量约 5.80 s；本机单次运行是性能参考。汇总证据见 `assets/rebar-control-net-2026-09-14/curved-v14-verification.json`，局部实测图见 `assets/rebar-control-net-2026-09-14/curved-v14.png`，剖析及回放日志在 `/tmp/cloudbim-v14/`。本轮由主智能体完成，前轮已结束的 sol high 子智能体未重新占用。

## 弯段求解提速（2026-09-14，v13）

[打开 v13 结果](http://127.0.0.1:8766/?run=20260914T102108-bad40603)。同一份 9,216,369 点源数据、相同粗对齐和 8 个上游工作进程预算，全流程由 **190.46 s 降到 46.49 s**。曲段阶段由 **152.43 s 降到 8.43 s**。尚未加入弯段的 v11 约 38.78 s，因此目前完整弯段处理增加约 7.7 s，而非原先约 152 s。

瓶颈是求解器用参数差分估计梯度时，反复执行整批点的最近曲线搜索。v13 保持相同模型、采样上限、初值集合、稳健损失和全部确认条件，改为复用最近线段及投影位置计算距离梯度；仅对几十个曲线顶点求小规模参数导数。法线轴线候选也预先计算，目标函数与梯度共享同一次投影。另将互不依赖的局部拟合按调用方预算分配给最多 8 个进程，每个进程只用一个数值计算线程；源点、主体状态均只读，最后仍由主进程按设计顺序执行唯一归属。

| 指标 | v12 | v13 |
| --- | ---: | ---: |
| 曲段阶段 | 152.43 s | 8.43 s |
| 控制网整体 | 180.64 s | 36.85 s |
| 全流程 | 190.46 s | 46.49 s |
| 已确认曲段 | 120 / 150 | 121 / 150 |
| 曲段新增支持点 | 33,610 | 33,781 |
| 曲段表面 RMSE 中位数 | 0.555 mm | 0.558 mm |

旧版确认的 120 个曲段全部保留，新增确认连接 #180 / #181。主体 210 条轴线、已有归属及台面和源坐标继续逐项相同。优化后的独立串行与 4 / 8 进程重放结果逐项一致；单进程曲段约 50.85 s，四进程约 14.63 s，八进程约 8.53 s，说明收益同时来自减少重复计算及有界并行。计时为本机同源运行参考，不是跨设备性能保证。

76 项算法与流程测试通过，新增梯度检查将复用投影的导数与独立参数差分比较；原有真实弯曲正例和噪声反例继续通过。sol high 独立审查验证了从后台线程启动、工作进程数量上限、每进程数值线程限制、父进程状态隔离，以及正常完成和工作进程异常后的全部回收；审查已完成，无遗留计算进程。正式输出的全量及子集 LAS、100 万点 HTTP 加载器及 Three.js 几何构建核验通过；使用与生产入口一致的 float64 法向重放后，曲段报告和逐点状态、归属与发布结果逐项相同。证据目录 `/tmp/cloudbim-curves-speed/`，汇总见 `assets/rebar-control-net-2026-09-14/curved-v13-verification.json`。v12 历史结果仍可打开比较。

## 局部参数化弯段（2026-09-14，v12）

[打开 v12 结果](http://127.0.0.1:8766/?run=20260914T100125-752684c6)，选择 **01B-X 台面后控制网**。可先看长筋弯钩 #3，以及腹杆 #64、#65、#66；#2 的弯钩仍待定。亮橙色曲线和管体表示已确认拟合，打开「初始化 / 设计」才显示未确认的设计弯曲线。旧 [v11 结果](http://127.0.0.1:8766/?run=20260914T084630-1abadfc7) 保留。

本次把原先未进入求解的曲段纳入控制网：从 IFC 保留有序直线、圆心、半径、法平面和转角，按已有设计单元之间的间隙、母筋两端生成 140 个连接弯段及 10 个弯钩。仍为 70 根物理母筋、210 个主体单元，曲段不另造母筋身份。设计弧线按 0.1 mm 弦误差重新采样，旧主体单元的编号和设计几何不变。

机制只有三个部分：

1. **主体先稳定，局部形状后调整。** 腹杆连接使用两段同半径圆弧及中间相切短直段，以切点范围、圆角比例两个参数表达；中间直段为零时退化为普通圆弧。它能表达扫描中略平的谷底。弯钩调整三个位置参数、弯曲平面转角、圆弧半径和转角，共六个参数；末端直腿保持设计长度。接入主体的过渡段由固定主体点和切向确定，不增加自由控制点。
2. **点云决定参数，设计限制形状。** 有效法线将表面点反推为两个轴线候选，以较近者形成不依赖法线正负的残差；缺少法线时使用固定钢筋半径的管面距离。弱尺寸先验、有界参数和稳健损失抑制离群点。局部裁剪内的直杆点由原有杆身共同解释，避免短弯段为了吸收两侧直杆观测而被错误撑大。
3. **确认曲段后才分配点。** 曲段必须具有连续轴向覆盖、截面变化、法向支持，并通过有间隔的空间分组复拟合；训练组独立选取初值和权重。还要检查尾段观测、形状歧义及唯一归属。已有主体归属不改，新增点沿用相邻单元身份；不足的曲段保留待定。

| 指标 | v12 正式运行 |
| --- | ---: |
| 主体拟合 | 210 / 210 |
| 腹杆连接弯段 | 114 / 140 |
| 长筋弯钩 | 6 / 10 |
| 曲段新增支持点 | 33,610 |
| 全量支持点 | 1,743,115 |
| 已确认曲段表面 RMSE：最小 / 中位数 / 最大 | 0.31 / 0.56 / 0.70 mm |
| 曲段求解 / 全流程耗时 | 152.43 / 190.46 s |

30 个待定曲段中，21 个缺少连续曲面支持，4 个分组复拟合不稳定，3 个存在相近形状候选，1 个尾段观测不足，1 个无法可靠连接主体。没有为了补齐设计数量放宽这些条件。早期较强尺寸先验试算为 124 个确认；最终降低先验对真实局部形状的偏置后，确认数为 120，以上均按正式保存结果统计。

![实际弯钩、平底连接及待定弯钩](assets/rebar-control-net-2026-09-14/curved-v12.png)

图中灰点为实际扫描、灰色虚线为设计轴线、橙线为拟合轴线、淡橙范围为钢筋半径包络。扫描往往只覆盖可见侧，因此轴线不会穿过表面点。RMSE 是已接受表面点的拟合残差，不是真值精度，也不能单凭支持点增加判定去噪质量。

主体的 `centerlineM`、设计长度和已有点归属与 v11 逐项相同；`bodyDisplayCenterlineM` 只用于连接处的显示裁剪，所有已确认曲段与显示主体的端点间隙为零。`curvedPieces` 单独记录确认状态、原因、设计曲线、拟合曲线和支持证据。弯钩的 `fittedLengthM` 包含接入主体的过渡段，不能直接与设计弯段长度相减当作超长量；界面明确提示遮挡处含模型插值。这里没有生成新扫描点，也未把过渡段全长宣称为实测。

验证：75 项算法、设计几何、主体及流程测试，13 项 HTTP 测试，24 项 Node 界面检查通过。独立反例覆盖平面、孤立截面簇、邻筋错接、竞争母筋、端球伪支持及特殊状态保护；新增正例覆盖真实偏移弯钩、带两侧直杆观测的平底弯段。921 万点的源坐标、法向、台面、旧归属和全部主体几何一致性检查通过。全量及各子集 LAS 的全部原始字段、来源索引与新归属核验通过；100 万点预览映射、真实 HTTP 加载器和 Three.js 曲段几何构建通过。本次 CUA 没有可连接浏览器，WebGL 页面交互未实测。

本轮按用户要求使用 sol high 子智能体分别完成精确设计几何、显示接入和独立反例检查，均已完成并停止写入；主智能体完成求解、集成和最终核验。保留的边界是单弯曲设计片段和有界局部变形，复杂多弯片段暂不自动降为单圆弧。当前优先保证弯段可靠性，尚未据此改动主体中部的自由度。可复核数据见同目录的 `assets/rebar-control-net-2026-09-14/curved-v12-verification.json`；详细运行脚本与日志位于 `/tmp/cloudbim-curves-v12/`。

## 腹杆的法向补充判别（2026-09-14，v11）

[打开 v11 结果](http://127.0.0.1:8766/?run=20260914T084630-1abadfc7)，仍在 **01B-X 台面后控制网**，不执行分区或分层。可与 [v10](http://127.0.0.1:8766/?run=20260914T083235-4819efb2) 比较。原先待定的 #78、#82、#84、#86、#88、#92、#106、#108、#122 全部恢复，拟合数量由 201/210 变为 210/210。

这九段的设计直段约 103.15 mm、半径 3 mm，局部扫描与设计初始方向存在差异，端部还叠有接头或相交结构。原始点投影到初始截面后，仅用径向距离计分会出现两个分数接近的圆心候选，在连续轴线求解前触发 `ambiguous-parallel-support`。实际候选快照每段约 3,300–4,300 点，可独立重放这个失败。增加到 768 近邻/站只恢复 8/9；加入径向法向证据，9/9 局部重放都通过。

v11 的机制是**歧义后的补充判别**：

1. 正常的固定半径拟合先照常执行。只有腹杆初始圆柱候选存在歧义，且至少有 24 个有效投影法向时，才额外判别一次。
2. 在完全相同的候选点和搜索边界内，圆柱候选的支持点同时满足径向残差与法向一致性。具体使用投影法向与圆心径向的绝对余弦不小于 0.8；法向不要求统一朝向。阈值沿用既有几何评分规则，不是工程验收容差。
3. 全局圆心假设和沿轴分段的连续证据使用同一法向条件，再执行既有固定半径、截面曲率、平面拒绝、有效轴向跨度、固定设计长度以及全量唯一归属检查。仍有歧义则保留待定。

补充判别没有改大候选范围、增加分层规则、放宽平行歧义阈值或按编号特判。统一对所有腹杆强制法向约束的试验会影响另外九段，因此最终只对未解决的歧义调用补充判别。缺少有效法向时保持原几何判定，不从设计数量推断成功。

报告的 `instances[].webEvidence` 记录补充判别方法、触发原因、有效法向点数和范围，练武场单元详情显示“经表面法向补充判别后通过拟合检查”。

| 指标 | 台面后 v10 | 台面后 v11 |
| --- | ---: | ---: |
| 拟合成功 | 201 / 210 | 210 / 210 |
| 支持点 | 1,698,616 | 1,709,505 |
| 待定点 | 2,201,727 | 2,190,838 |
| 局部离群点 | 30 | 30 |
| 短筋长度待核查 | 4 | 4 |
| 控制网耗时 | 27.35 s | 28.86 s |
| 全流程耗时 | 36.76 s | 38.78 s |

新恢复九段的径向 RMSE 为 0.100–0.173 mm；与历史分层后轴线最大端点差约 0.55 mm。原有 201 段中 199 段轴线逐项相同；错位短筋 #49、#58 的恢复过程使用未归属点，新增腹杆支持改变了这部分候选，轴线分别变化约 1.58、1.23 mm，设计长度仍为 280 mm，长度待核查标记保留。新增支持数、低残差及接近旧轴线均不等于真值精度；计时仅为本机参考。

![九段腹杆的实际局部拟合](assets/rebar-control-net-2026-09-14/web-normal-evidence-v11.png)

验证：41 项 Python 算法/流程检查、15 项 Node 检查通过。新增反例覆盖斜腹杆的部分圆弧与密集切向杂点、法向符号翻转、真实双平行腹杆、平面夹具以及缺少法向。正例在 v10 被拒绝，v11 保留全部 1,512 个真实圆柱样本且不归属 2,016 个杂点；其余反例仍保留待定。

921 万点集成运行的台面掩码、未分层边界、状态守恒、唯一归属、固定长度、预览索引和实际 HTTP 加载器检查通过。发布的逐点状态/归属与独立全量试算完全相同；全量 LAS 和所有子集逐字段、逐来源索引核验。本次没有可连接的浏览器，未新增 WebGL 交互验证；上图来自实际拟合数据。证据目录 `.cloudbim/control-net-web-v11/`。sol high 子智能体完成独立反例测试并已停止写入，无后台进程；主智能体完成机制、全量验证、界面说明与结果保存。

## 台面后无分层试跑（2026-09-14，v10）

[打开本次结果](http://127.0.0.1:8766/?run=20260914T083235-4819efb2)，在 **01B-X 台面后控制网** 查看，或从历史记录切回 [分层后 v9](http://127.0.0.1:8766/?run=20260914T073939-934a0870)。本次使用保存的粗对齐、8 个上游线程、1 个拟合线程；自动初始化路径保留并经算法回归检查，本次全量比较未运行自动模式。

| 指标 | 分层后 v9 | 台面后 v10（无分层） |
| --- | ---: | ---: |
| 源点数 | 9,216,369 | 9,216,369 |
| 非台面候选点 | 3,900,373 | 3,900,373 |
| 拟合成功单元 | 210 / 210 | 201 / 210 |
| 支持点 | 1,692,624 | 1,698,616 |
| 待定点 | 2,207,719 | 2,201,727 |
| 局部离群点 | 30 | 30 |
| 短筋长度待核查 | 4 | 4 |
| 拟合耗时 | 29.43 s | 27.35 s |
| 全流程耗时 | 40.15 s | 36.76 s |

#78、#82、#84、#86、#88、#92、#106、#108、#122 由已拟合变为待定，均触发 `ambiguous-parallel-support`。无层约束时空间邻域内的候选组成发生变化；当前保持歧义保护，不以目标数量强行确认身份。可以在「设计单元」中逐个查看其原始邻域，打开「显示初始化控制网」辅助定位。

支持点增加 5,992 不表示精度提高：旧待定中有 26,439 点成为支持点，旧支持中有 20,447 点转为待定。两版均拟合成功的单元中，同采样位置的轴线最大变化出现在 #64（11.42 mm）、#105（8.79 mm）、#51（6.49 mm）；这是版本间几何变化，不是真值误差。#49、#51、#56、#58 仍需核查短筋可见长度。计时为本机同源试跑，非严格性能基准。

![分层后与台面后控制网对比](assets/rebar-control-net-2026-09-14/post-table-v10.png)

验证：48 项 Python 算法、流程及台面/共享场景检查，13 项 HTTP 检查，15 项 Node 检查通过。全量台面掩码与 v9 逐项一致，未产生分区、层、禁飞区或分类融合报告；全量状态守恒、唯一归属、设计长度及预览来源映射通过。实际 HTTP 结果通过练武场加载器（210 个报告单元、100 万预览点），无分层筛选。全量 LAS 与各子集按每个原始字段和来源索引核验。

本次没有可用的 CUA 浏览器，未实测 WebGL 页面渲染与交互；上图是从真实结果数据生成的静态对比图。sol high 子智能体负责算法与回归，已停止写入且无后台进程；其余流程、界面与集成验证由主智能体完成。证据目录 `.cloudbim/control-net-post-table/`，包含比较、加载器验证、LAS 验证和子任务交接记录。

## #33 的采样歧义误拒绝修正（2026-09-14，v9）

[查看修正结果](http://127.0.0.1:8766/?run=20260914T073939-934a0870)，选择设计单元 #33。该单元为底层横向直筋，直径 8 mm、设计长度 1.15 m。拟合轴线和半透明圆柱使用亮橙色，便于与蓝色点云区分。

v8 的 `ambiguous-parallel-support` 表示初步圆柱候选存在歧义，并不证明现场有两根钢筋竞争。#33 在设计轴线每站最近 192 点的采样下，只获得偏向近侧的截面证据：5,184 个候选经轴向/法向筛选剩 3,380 点，前两组投影圆心相距约 6.08 mm、评分分别为 462.86 和 459.63，触发早期拒绝。它们的支持点几乎全部原已被融合识别为钢筋，不能把这一现象简单归因于夹具误分类。该钢筋轴线相对设计位置约偏移 16–18 mm，仍在原 100 mm 搜索范围内。

以同一半径、同一层和同一空间范围增加候选后，384、768、1,536 点/站均能恢复连续拟合；仅开启法向一致性或直接跳过歧义检查，分别出现证据不足或仅约 0.33 m 的局部支持，故未采用这些方案。v9 只对原先因平行候选歧义失败、尚未使用较多采样的普通直筋，追加一次 768 点/站的求解；仍限制每单元最多 30,000 个拟合候选，保留全部圆面、连续性、平面、歧义、重复身份和全量唯一归属检查。成功报告的 `candidateSampling` 记录这次补充取样。不按 #33 编号特判，不扩大设计位置搜索范围，也不放宽拟合验收阈值。

全量 9,216,369 点重跑后，#33 由待定转为已拟合，支持 13,268 点、径向 RMSE 0.442 mm、连续观测范围约 0.994 m；控制网恢复 210/210 个单元。其他 209 个单元的中心线逐项相同，#4/#6/#10/#11 各有 1/3/11/30 个交叉附近支持点重新归属给 #33；另外 13,223 点来自原待定集合。支持点总量为 1,692,624，待定 2,207,719，局部离群仍为 30。台面、分区、分层和原始数据完全相同，未执行禁飞区或融合。控制网 29.43 s，全流程 40.15 s，均为本机参考计时。拟合数量、残差以及与旧轴线接近均不是人工真值精度。

验证包含先失败后通过的“16 mm 偏移、半圆弧、轻微弯曲、密集采样”回归，以及三种间距的真实双平行候选持续待定检查。47 项 Python 算法/轴线/流程/导出测试、14 项界面回归通过；全量状态、实例归属、预览来源索引和全部 LAS 原始属性逐项核验。浏览器验证 #33 橙色拟合、点集切换与 v8 历史加载，无页面错误。排查脚本、数值证据和截图位于 `.cloudbim/control-net-33-task/`，未向生产代码添加调试日志。

## 前移试跑（2026-09-14）

[查看新结果：01E 分层后控制网](http://127.0.0.1:8766/?run=20260914T072309-18d46aba)，可用历史下拉框切回 [原融合后 v8](http://127.0.0.1:8766/?run=20260914T065844-07a7193c)。本次实测使用粗对齐模式、8 个上游线程、1 个拟合线程；拟合算法均为 v8，仅改变输入阶段。

| 指标 | 原融合后 | 分层后、禁飞区前 |
| --- | ---: | ---: |
| 源点数 | 9,216,369 | 9,216,369 |
| 非台面拟合候选 | 2,009,509 | 3,900,373 |
| 已拟合设计单元 | 210 / 210 | 209 / 210 |
| 底层 / 顶层 / 腹杆 | 64 / 2 / 144 | 63 / 2 / 144 |
| 已归属支持点 | 1,674,954 | 1,679,401 |
| 待定点 | 334,548 | 2,220,942 |
| 局部离群点 | 7 | 30 |
| 融合排除点 | 1,890,864 | 不执行融合 |
| 短筋长度待核查 | 4 | 4 |
| 拟合耗时 | 28.31 s | 28.90 s |
| 全流程耗时 | 55.37 s | 39.43 s |

#33 从拟合成功变为待定，原因为 `ambiguous-parallel-support`（邻近钢筋匹配歧义）；其他单元的拟合状态不变。新支持点中 19,474 点原被融合标为夹具，1,659,927 点原为钢筋；这些差异用于定位复核，原分类不是人工真值，支持点数量不能作为准确率。候选范围扩大后，待定点包含原先被融合排除的夹具等记录，所以待定数量不宜直接解释为钢筋漏检增加。

全流程加快主要因为省去禁飞区和分类融合，拟合本身没有加快。该计时是本机同源观察，非严格性能基准。可在「设计单元」选择 #33 检查其邻域，并用短筋定位按钮复核 #49、#51、#56、#58。

验证：62 项 Python 算法/边界回归、13 项 HTTP 检查、15 项 Node 检查通过。新旧台面、分区、分层的全部原始记录逐项相同；新结果无禁飞区/分类/融合报告，全量状态守恒、实例归属与预览来源映射通过。浏览器检查了分层筛选、点集切换、短筋定位、手机宽度以及新旧历史切换，无页面错误。全量 LAS 和各子集的每条原始属性、来源索引已逐项核验。证据目录为 `.cloudbim/control-net-layering-task/`。

首次保存因磁盘不足失败，未发布不完整结果；清理两份旧 pip HTTP 下载缓存后重新运行成功，源数据与历史运行未删除。

## 长度核查与噪声弯曲保护（2026-09-14，v8）

最新结果：[20260914T065844-07a7193c](http://127.0.0.1:8766/?run=20260914T065844-07a7193c)。v7 为恢复错位短筋，允许将模型加长至设计长度的 150%。这是候选搜索规则，不是构件长度验收容差；仅凭圆柱邻域点的 1%–99% 投影跨度，不能认定实际钢筋超长。遮挡、相交钢筋、端部杂点和设计身份不一致均可能影响这个数值。

**v8 撤回自动加长。** 四根找回短筋的 `designLengthM`、`fittedLengthM` 均为 280 mm。`observedLengthM` 保留观测跨度，差值超过 `max(10 mm, 10% 设计长度)` 时写入 `lengthCheck: review-observed-span`。该阈值用于提醒复核，不是工程允许偏差。界面显示“位置已匹配，长度待核查”，不会把位置拟合成功当作尺寸合格。短筋尾部未归属点继续保留。

跨度估计增加径向法向一致性检查和 10 mm 轴向分箱密度检查，稀疏、恰好靠近圆柱的散点不能撑长端部。当前四根的观测跨度约为 #49 315 mm、#51 336 mm、#56 377 mm、#58 359 mm，均需核查实际端点与设计身份，不作为已确认的真实长度。

普通长筋的轴线拟合增加以下约束，腹杆和短筋主体继续使用直线圆柱：

1. 按轴向窗口平衡证据时，设置样本数下限，避免少量端部噪点被放大到与密集主体等权。
2. 可靠端部范围要求至少三个相邻窗口有足够支持；范围外仅延续端部切线，禁止三次样条自由外推。
3. 依次比较直线、二次曲线、三次样条。用各窗口的中位残差和支持率衡量改进；局部密集杂点不能靠数量支配整根轴线。简单模型已足够时保留简单模型，连续观测支持的真实弯曲继续保留。

共享 `fit_prior_axis` 的新选项 `guard_bending` 仅由控制网普通长筋启用，其他既有调用默认保持原路径。报告使用 `axisModel` 区分直线、简单弧形和受观测约束的样条，`axisEvidenceRangeM` 记录轴向证据范围；这不是测量置信区间。

| 指标 | v7 | v8 |
| --- | ---: | ---: |
| 有拟合支持的单元 | 210 / 210 | 210 / 210 |
| 短筋长度待核查 | 未单列 | 4 |
| #1 轴线相对端点连线的最大偏离 | 25.03 mm | 9.25 mm |
| #6 同一指标（连续实测弯曲） | 16.22 mm | 16.18 mm |
| 已归属原始点 | 1,710,087 | 1,674,954 |
| 待定点 | 299,415 | 334,548 |

#1 的端部有大量无观测区域和少量离散点，旧样条被拉弯；v8 减小了这一影响。9.25 mm 是模型几何指标，不是真实偏差或准确度证明，剩余外推仍属推断。#6 的连续弯曲有密集表面支持，不能仅为视觉平直而抹去。54 个普通长筋中，10 个选择直线、24 个选择二次曲线、20 个保留受约束样条。

![原始融合候选与 v7/v8 轴线对比](assets/rebar-control-net-2026-09-14/noise-guard-v8.png)

此次 9,216,369 条源记录的台面、分层、融合数组与 v7 完全一致。局部离群仍为 7 点，融合排除仍为 1,890,864 点；归属减少的点保留待定。控制网 28.31 s，全流程 55.37 s，为本机参考计时。拟合数量不代表人工真值准确率，也不代表四根短筋尺寸已通过核查。

验证：43 项 Python 算法/共享求解/流程/导出测试，22 项 Node 界面/几何回归，13 项 HTTP 测试通过。新增测试覆盖稀疏端部杂点、密集中段污染、连续真实弯曲、切线外推、超长短筋固定模板和长度核查。全量状态、归属、预览、四类 LAS 及全量 LAS 的原始字段和顺序已逐项核验；浏览器验证了四根短筋的 280 mm 模型、长度待核查提示、定位循环、144 个腹杆直段与移动端。证据目录 `.cloudbim/control-net-robust-task/`。

## 错位短筋找回与腹杆直段约束（2026-09-14，v7，长度规则已被 v8 替代）

最新运行：`20260914T063735-daf91abf`，入口 [练武场结果](http://127.0.0.1:8766/?run=20260914T063735-daf91abf)。进入 **03X 分层控制网**，点击 **定位错位 / 未拟合短筋**，依次检查 #49、#51、#56、#58。按钮同时显示设计初始化和实际拟合，并把两者纳入视野；重复点击循环定位。详情分别显示中心错位、观测长度、设计长度与拟合长度。

原搜索沿设计直段取近邻，且短筋轴向中心必须落在原设计区间内。这四根实际偏移约 0.4 m，原局部窗口主要取到相交长筋与平面点。x≈2.9 m / 5.5 m 处已有短筋不能再次借给缺失单元；从已拟合表面以外的点中，才找到 x≈3.1 m / 5.3 m 处的实际候选。

- 先完成原分层拟合，再只对未确认短筋搜索。候选限于融合钢筋，排除已有模型的支持点；横向范围 0.50 m、轴向端部余量 0.18 m，每段求解最多 30,000 点。
- 从观测线特征估计平移和方向，固定设计半径拟合直线圆柱。检查连续有限区间、平面/法向证据、可见跨度、搜索边界、候选竞争与最终唯一归属。近似候选评分达到最优的 90% 时保留待定；不能从长筋截取一段来凑数。
- 找回短筋的连续可见跨度超过设计长度 110% 时，以独立字段 `fittedLengthM` 记录实测拟合长度，最大接受设计长度的 150%；`designLengthM` 和设计库存不变。普通段仍使用设计长度。该阈值为实验搜索范围，不是工程验收容差。
- 腹杆设计直段改为直线圆柱参数模型，固定半径和设计直段长度，只求位置、倾角；用轴向 12%–88% 的主体点估计轴线，避免末端弯曲、接头和交叉筋把主体拉弯。最终全部邻域原始点仍参加归属判断。自由样条仍用于普通长筋。

| 指标 | v6 | v7 |
| --- | ---: | ---: |
| 已拟合单元 / 设计单元 | 206 / 210 | 210 / 210 |
| 腹杆直段 | 144 / 144 | 144 / 144（主体直线） |
| 已归属原始点 | 1,667,474 | 1,710,087 |
| 待定候选点 | 342,028 | 299,415 |
| 控制网阶段 | 29.71 s | 27.59 s |

| 找回短筋 | 中心错位 | 设计长度 | 拟合长度 | 径向 RMSE | 支持点 |
| --- | ---: | ---: | ---: | ---: | ---: |
| #49 | 399.7 mm | 280 mm | 320.1 mm | 0.457 mm | 5,478 |
| #51 | 405.9 mm | 280 mm | 364.8 mm | 0.548 mm | 5,435 |
| #56 | 407.6 mm | 280 mm | 378.4 mm | 0.262 mm | 6,623 |
| #58 | 388.6 mm | 280 mm | 363.5 mm | 0.239 mm | 5,885 |

![错位短筋的原始点、设计位置与找回轴线](assets/rebar-control-net-2026-09-14/short-recovery-v7.png)

![腹杆主体轴线修正前后与附近原始点](assets/rebar-control-net-2026-09-14/web-straight-v7.png)

同源 9,216,369 条记录，上游台面、分层、融合数组与 v6 逐项相同；融合排除 1,890,864 点、局部离群 7 点不变。全流程 64.17 s，包含上游阶段和持久化；控制网耗时是同机参考，不代表跨机器性能。210/210 是约束检查后的拟合数量，不是人工真值准确率。

本版实现腹杆主体直线约束，**尚未联合求解相邻腹杆的圆弧接头**。曲端和未建模接头的稠密点继续保留，不会仅因偏离直线而删除，也不把设计圆弧显示成已测得的拟合。短筋完整可见区间来自点云；遮挡端点仍可能影响长度估计，设计身份也应结合实际装配复核。

验证记录在 `.cloudbim/control-net-short-task/`：38 项 Python 算法/共享轴线/流程/导出测试、12 项控制网界面测试及相邻界面回归、13 项 HTTP 测试；包括 100 mm 轴向错位、400 mm 横向错位、已匹配邻筋保护、实测超长、长筋截段拒绝、多候选歧义和弯端不拉弯主体。全量状态、逐点实例、预览来源索引、四类非台面 LAS 与全量 LAS 的每条源属性均已核对。真实浏览器通过四根短筋循环定位、两处几何同时入镜、144 段腹杆直线、点集切换及移动端检查，无页面运行错误。历史 v6 和早期报告继续按其原有长度契约加载。

## 中间漏检与邻筋混拟合修正（2026-09-14，v6）

运行 `20260914T024209-3487f12c`，同一源文件、同一融合与分层结果。中间缺失长筋为单元 #6；腹杆下方重点检查 #3/#61 和 #4/#63。v5 中 #4 与 #63 在共同长度内拟合到同一条可见圆弧；长筋多出的端部还掩盖了单向平均距离的重复判断。

本版采用以下修正：

- 每个轴向窗口保留至多 8 个不同圆心，通过连续路径选择支持点；横筋法线干扰与长筋截面的法线/圆面一致性参与筛选。
- 长于 2 m 的直筋，每个查询站候选从 192 增至 768，总候选仍限制为每单元 30,000 点，防止较近的筋占满候选集。
- 对长度不同、近乎平行且主要范围重叠的邻筋，以较短筋已经拟合出的观测表面排除长筋拟合中的借点，再拟合长筋。此排除仅用于求解，所有原始候选点仍参加最终归属竞争。
- 将窗口中测得的圆心作为额外 B 样条初值，按真实表面残差择优，避免弯曲钢筋的局部可见圆弧落入镜像圆心。Scan-vs-BIM 的原有调用不传该可选初值，原求解路径保持不变。
- 平行重复轴线从两个方向检查，较长筋的伸出部分不能掩盖共同长度内的重复。

| 指标 | v5 | v6 |
|---|---:|---:|
| 确认直段 / 设计直段 | 203 / 210 | 206 / 210 |
| 确认原始点 | 1,301,845 | 1,667,474 |
| 待定候选点 | 707,657 | 342,028 |
| 单元 #6 | 未拟合 | 59,803 点，径向 RMSE 0.482 mm |
| 控制网阶段 | 23.72 s | 29.71 s |
| 全流程 | 50.88 s | 56.82 s |

剩余 #49、#51、#56、#58 保持待定（平面法线/短筋区间证据问题），不补造观测。融合排除 1,890,864 点、台面 5,315,996 点、局部离群 7 点；上游台面、分层及融合数组逐行相同。更充分的候选与连续求解增加约 6 s，属于质量修正的实际代价。

在 X=3.91–3.93 m 的原始截面中，分别对四条可见圆弧独立进行二维圆拟合，用于局部诊断。#3、#61、#4 的连续轴线与局部圆心距离由 7.31、10.42、15.66 mm 降至 0.257、0.872、0.312 mm；#63 由 0.695 变为 0.836 mm，仍贴合原圆弧。这些是局部截面检查，不是全钢筋人工真值精度。

![邻筋原始截面与修正前后拟合圆](assets/rebar-control-net-2026-09-14/neighbour-sections-v6.png)

验证：控制网、共享轴线求解、流水线及 LAS 单元测试共 34 项通过；包含不等长邻筋点归属、长端部掩盖重复、弯曲部分圆弧镜像解的回归。全部 9,216,369 行状态与预览映射、四个非台面 LAS 以及全量 LAS 的所有源属性及来源索引均逐项核对。练武场粗对齐运行、分层筛选、单元切换、各点集视图和手机宽度检查通过，页面无运行错误。

## 性能优化（2026-09-14，v5，历史）

`design-control-net-v5` 保持 v4 的候选、搜索次数、局部窗口、验收阈值和点归属规则，优化圆心投票的数组组织与分组。没有跳过弯曲检查或改用近似圆心。投票改为整块数组拼接；二维整数量化网格编码成保持字典序的一维整数，替代较慢的二维行排序；投票同分时的候选顺序保持一致。极端整数范围回退到原二维分组，避免溢出。

同源缓存分项实测：圆心搜索仍为 5,178 次，但耗时从 27.24 s 降到 10.72 s；整个控制网从 40.39 s 降到 23.63 s。整段轴求解仍约 5.62 s。原 Scan-vs-BIM 的 23.24 s 是约 184 万已分簇点的求解计时，不能与包含候选搜索、重复检查和全量归属的控制网阶段视为完全相同工作。

最终通过练武场 HTTP 串行完整运行：

| 模式 | 最终运行 | 控制网 v4 → v5 | 全流程 v4 → v5 |
| --- | --- | ---: | ---: |
| 粗对齐 | `20260914T020850-e925e3ab` | 40.21 → 23.72 s | 67.42 → 50.88 s |
| 自动初始化 | `20260914T020719-60d77711` | 26.22 → 16.77 s | 53.24 → 43.83 s |

粗对齐控制网耗时减少约 41.0%，全流程减少约 24.5%。这仍是同机参考数据，使用 8 个上游工作线程、1 个拟合线程，OS 缓存未清空。

两条新运行均逐项对照各自 v4：全部 9,216,369 行台面、分层、融合类别、控制网状态与实例编号完全相同；逐段模型、中心线、库存和配准报告也完全相同。粗对齐仍为 203/210 段、1,301,845 个确认点、707,657 个待定候选、7 个局部离群点及 1,890,864 个融合排除点。自动模式仍保留网格歧义，不写确定编号。

24 项算法/流程/导出测试通过，包括投票负坐标、计数并列顺序、极端整数范围及原有弯曲/平面/邻筋/源记录保护回归。两条最终运行都通过完整四类 LAS 原始属性、来源索引、预览、状态守恒和中心线弧长校验，并通过真实浏览器分层/逐段/导出/移动端检查。证据在 `.cloudbim/control-net-speed-task/`：`compared-aligned.json`、`compared-auto.json`、`verified-aligned.json`、`verified-auto.json`。

## 分层版本验证（2026-09-14，v4，历史）

最终从原始 LAS 经本轮 03 融合运行，两条路径串行，通过练武场 HTTP 提交。源文件为同一份 9,216,369 点；台面 5,315,996 点，03 融合钢筋候选 2,009,509 点，融合排除 1,890,864 点。旧完整流程末端的 1,858,032 个钢筋点不是 03 融合候选数量。

| 路径 | 运行 | 确认直段 | 支持点 | 待定候选点 | 局部离群 | 控制网 / 全流程 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| 粗对齐、分层 | `20260913T174532-66060cae` | 203/210 | 1,301,845 | 707,657 | 7 | 40.21 / 67.42 s |
| 自动初始化、分层 | `20260913T174349-570fffd3` | 0/210 | 0 | 2,009,509 | 0 | 26.22 / 53.24 s |

粗对齐：底层 58/64，顶层 2/2，腹杆 143/144。相较 v3 的腹杆 138/144，增加 5 段；全部单元有 7 段新确认、2 段从已拟合转为未确认，净增 5 段。底层仍有 6 段未确认，数量未改善，不应声称已经消除底层漏检。剩余单元编号 6、31、49、51、56、58、139，分别涉及设计半径不一致、邻筋竞争、平面法向证据和短筋区间歧义。已编号支持点增加不是独立真值召回率；旧版本曾支持的第 6 段，在诊断中需要混入融合排除的点才能拟合，故新版本保留不确定性。

自动初始化保留 141 条待确认候选轴线；不同候选相差约 100 mm，评分接近，所以仍不写确定身份。其输入已经经过使用保存对齐先验的上游分层和融合，不能当作完全无配准的实验。

性能对照：同一缓存源数据、单线程 BLAS，原 v3 后台面拟合复测 95.745 s；最终 v4 完整流程中的拟合 40.209 s，减少约 58.0%。这是本机同源测试，OS 缓存未清空，服务仍运行；并非跨硬件性能保证。全流程 67.42 s 包含新增的融合前处理，不能仅以拟合阶段降幅代表整体降幅。

加速来自分层候选索引、向量化圆心投票、仅在模型候选并集上竞争以及一次直方图统计归属。分层初拟合不通过时，仅在原有设计直段邻域对融合钢筋候选进行一次重试，所有半径、平面、歧义和唯一归属检查不变。8 个逐筋拟合线程反而变慢，已停止该试验；生产控制网固定单线程，界面线程选项继续用于上游法向量/分类。

离群点的原始邻域保护仍使用全部非台面点，包括融合排除点；最终复核让 54 个稠密邻域边缘点保留待定，仅有 7 点满足稀疏局部离群条件。所有融合排除点都保存在全量文件和独立 LAS 中。

根代理验证 22 项算法/流程/导出测试、13 项 HTTP 检查、11 项重点界面测试通过；相邻界面 13 项通过。两条最终运行的全量状态守恒、实例归属、设计弧长、逐条 LAS 原始属性和预览索引通过校验。浏览器检查了两条最终运行与旧 v3 历史，包括语义层、排除点、逐段查看、下载入口及移动端布局。证据在 `.cloudbim/control-net-fusion-task/`，最终全量报告为 `verified-aligned.json`、`verified-auto.json`。

## 早期版本验证记录（台面移除后，v3）

2026-09-14 北京时间，最终算法 `design-control-net-v3`，源数据 9,216,369 点；台面移除 5,315,996 点，候选 3,900,373 点。两个运行独立从原始 LAS 开始，以各 8 个法向量线程并行离线计算；下列耗时不代表经过控制变量的性能基准。

| 路径 | 运行 | 已确认拟合直段 | 待确认候选轴线 | 已编号支持点 | 待定点 | 局部离群点 | 全流程 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 已有粗配准 | `20260913T171858-40a82207` | 198/210 | 0 | 1,229,008 | 2,671,335 | 30 | 108.08 s |
| 自动初始化 | `20260913T171858-4cc539d2` | 0/210 | 182 | 0 | 3,900,373 | 0 | 103.35 s |

粗配准路径尚有 12 段未确认：2 根长直筋、4 根短筋、6 个腹杆直段，主要因为邻筋竞争、截面/法向证据不足和短筋区间不一致。所有未确认点保留。自动路径检出 32 个线特征，最优位姿有 17 个一一对应支持；与另一位移约 100 mm 的候选评分只差约 3.3%，所以没有强行把候选轴线的身份写成确定编号。

与旧流程 `20260913T165730-72d3cf2c` 的分类仅作差异定位：早期支持点中 1,202,685 点旧标为钢筋、17,702 点旧标为夹具、8,621 点旧标为噪声；另有 655,341 个旧钢筋点仍待定。旧结果不是独立真值，这些数量不能当作准确率、召回率或确认误差。当前结果仍需要检查错位短筋、端部弯钩以及夹具附近的邻筋竞争，不能据此直接替换正式流程。

全量验证检查了两个运行的状态分区守恒、台面掩码完全一致、实例只指向可靠拟合、所有拟合/候选中心线弧长等于设计长度、预览与原始记录对齐，以及三个 LAS 子集的每条原始属性逐项不变。确认拟合的弧长最大误差约 1.8e-15 m；这只是约束满足程度，不是实测准确度。

回归命令：

```bash
cd services/mesh-service
../../.cloudbim/mesh-venv/bin/python -m unittest test_rebar_control_net.py test_control_net_pipeline.py test_cylinder_denoise_artifacts.py
cd ../..
.cloudbim/mesh-venv/bin/python scripts/test_pointcloud_debug.py
node --test scripts/test_pointcloud_control_net_viewer.mjs scripts/test_pointcloud_cylinder_viewer.mjs scripts/test_pointcloud_shared_scene_viewer.mjs scripts/test_pointcloud_floating_noise_viewer.mjs
```

15 项 Python 算法/流程/导出检查、13 项 HTTP 检查、16 项 Node 界面/几何检查通过。浏览器另外验收真实百万点预览、可见控件、逐段附近原始点、候选轴线、三类导出链接、历史切换和移动端布局。证据与截图位于忽略目录 `.cloudbim/control-net-task/`，全量校验为 `verified-final.json`。

协作使用三名 `gpt-5.6-sol/high` 子智能体，分别实现核心、界面并独立审查。三者均完成且停止写入/后台任务；运行时未提供关闭子智能体工具，未宣称会话槽位已经释放。根代理接管后修复审查发现的问题并完成真实数据和浏览器验收，详见 `lifecycle.md`。

## 实现范围与判读

当前使用“整体几何定位 → 分类型固定半径拟合 → 未归属点中的短筋位姿搜索 → 全量点归属与重复/歧义检查”。腹杆和短筋主体使用直线圆柱，普通长筋使用平滑轴线。设计直段之间的整体相对关系用于位姿候选评价，局部允许偏移和缓慢弯曲；尚未实现所有母筋节点共同参与的弹性拓扑优化。弯钩当前不被强制拉到设计方向，也没有把钩部滚转当作已经测出的量；没有可靠直段支持的钩部点保留待定。

拟合采样与最终点归属严格分离：限制候选数仅用于求模型，模型建立后对其附近全部原始点重新判断，不把采样点数当作钢筋全量点数。局部离群点还需缺少原始邻域支撑；相邻密集表面、未建模交叉筋和旋转钩部不能仅凭圆柱残差被移除。

自动定位的一条观测线只能支持一个设计单元。若另一明显不同的位姿候选评分达到最优的 90%，则身份待定，不写确定实例编号。这个分数间隔是保守实验阈值，不是统计置信概率。仍可查看橙色虚线候选轴线（`candidateCenterlineM`），但它们不计入 `fittedUnits`，不生成已编号钢筋点，也不用于删点。

初步实测相近候选相差约一个网格间距，说明规则钢筋网仅靠整体几何可能无法唯一确定位置和身份。此时已有粗配准能提供有用的初始位置约束，但最终编号仍需检查短筋错位、邻筋竞争等情况。
